# Tests

This folder contains a console flow (`console_test.sh`) that mirrors professor-style endpoint checks.

## Usage

1. Start all services (`controller`, `scheduler`, `workers`, `api`) as documented in `user-guide.md`.
2. Place any PNG file at `tests/sample.png`.
3. Run:

```bash
bash tests/console_test.sh
```
